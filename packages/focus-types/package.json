{
  "name": "@overdraw/focus-types",
  "version": "0.0.0",
  "private": true,
  "description": "Shared canonical type definitions for the focus plugin namespace, imported by core (the focus driver) and any plugin claiming the 'focus' namespace. Exports a small runtime list (FOCUS_REASONS) so trust-boundary validators can derive from the same source as the FocusReason type.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  }
}
