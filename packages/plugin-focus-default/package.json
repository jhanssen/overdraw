{
  "name": "@overdraw/plugin-focus-default",
  "version": "0.0.0",
  "private": true,
  "description": "Bundled focus plugin: implements follow-pointer and click-to-focus policies. Ships with overdraw and registers in the 'focus' namespace at priority 0 (the floor of the priority chain). User configs may override by registering a higher-priority focus plugin in the same namespace.",
  "type": "module",
  "main": "./dist/index.js",
  "exports": {
    ".": {
      "default": "./dist/index.js"
    }
  },
  "dependencies": {
    "@overdraw/focus-types": "*",
    "@overdraw/plugin-sdk-types": "*"
  }
}
