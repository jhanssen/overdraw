# Throwaway de-risking experiments; not tracked.
/spikes/

# Build outputs: tsc dist/ and CMake build/ per package.
packages/*/dist/
packages/*/build/
# Also catch any stale root-level dirs from before the monorepo restructure.
/dist/
/build/

# Generated protocol modules (gen-protocol).
packages/*/src/protocols-gen/

# Node
node_modules/

# Editor / OS
*.swp
*~
.DS_Store

# gdb session history (from ad-hoc debugging)
.gdb_history

# TypeScript project-references incremental build info.
*.tsbuildinfo
